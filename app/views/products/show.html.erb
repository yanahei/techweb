<div class="contents row">
  <div class="container">
  <h4>ソフトダウンロードページ</h4>
  <% @products.each do |product| %>
    <p>※ 以下のボタンをクリックするとダウンロードが開始されます</p>
    <p><%= product[:created_at].strftime('%Y年%m月%d日 更新') %></p>
    <p><%= button_to product.filename, product_download_path(product.id), method: :get, class: "btn btn-info btn-lg" %></p>
  <% end %>
  </div>
</div>
